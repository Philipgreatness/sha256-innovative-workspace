[project]
name = "sha256-innovative-workspace"
authors = ["SHA256 Innovations Team"]
description = "A blockchain-powered platform for secure, privacy-preserving health data management"
telemetry = false
requirements = []
[contracts.health-access-control]
path = "contracts/health-access-control.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
